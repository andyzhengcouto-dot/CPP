#include <bits/stdc++.h>
using namespace std;
 
int main() 
{
  // Latest Version Recommend reading the DMOJ problem though
  
  double number;
  
  cin >> number;
 
  double result = sqrt(number);

  cout << "The largest square has side length " << floor(result) << "."<< endl;
 
   
  return 0;
}
